import { createAction } from "@ngrx/store";

export const increment = createAction('[Couter] Increment');
export const increment2 = createAction('[Couter] Increment2');
export const decrement = createAction('[Couter] Decrement');
